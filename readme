This container builds the uboot image required for ITRON HAB01

The build uboot image is available on the container at

/home/itron/u-boot/u-boot.bin

#build the container
docker build . -t mediatek_u-boot

# running this container
docker run -i -t mediatek_u-boot /bin/bash

